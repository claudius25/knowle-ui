services:
  - type: web
    name: knowle-ui
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: dist/knowle/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
